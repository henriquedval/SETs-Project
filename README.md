Steps before running your code:

1 -> Download the files on the folder karteen, available on this repo, make sure to preserve the filename.

2 -> Change the file directory on 
        folder = Path(r"your_directory_with_the_file_image")

3 -> Run the algorithm and enjoy your game!

---

The rules are contained within the Python file. 
